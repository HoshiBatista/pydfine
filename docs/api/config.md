# DFINEConfig

Every model + training option as one typed, frozen dataclass. Build with a size
[`preset`][dfine.config.DFINEConfig.preset] or construct directly; optional YAML
interop via [`to_yaml`][dfine.config.DFINEConfig.to_yaml] / `from_yaml`.

::: dfine.config.DFINEConfig
