## What & why

<!-- One or two sentences. Link the roadmap task this addresses. -->

Roadmap task: <!-- e.g. Phase 2 — DFINE.predict() returns Results -->

## Definition of Done (AGENTS.md §9)

- [ ] Code typed, formatted, lint-clean (`ruff format . && ruff check .`).
- [ ] Test added and passing (`pytest -q`).
- [ ] No YAML on the user's critical path; no registry/`create()` calls.
- [ ] Public param names match `docs/CONFIG_REFERENCE.md`.
- [ ] For model modules: loads matching upstream weights **or** has a documented remap.
- [ ] Roadmap checkbox ticked; `docs/*` updated if params changed.

## Notes for reviewers

<!-- Anything surprising, follow-ups, or decisions worth flagging. -->
